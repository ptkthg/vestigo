version: "3.9"

services:
  gateway:
    build: ./gateway
    ports:
      - "8000:8000"
    environment:
      - PARSER_URL=http://parser-service:8001
      - ENRICHER_URL=http://enricher-service:8002
      - AI_URL=http://ai-service:8003
      - INTERNAL_API_SECRET=${INTERNAL_API_SECRET}
      - RATE_LIMIT_PER_MINUTE=${RATE_LIMIT_PER_MINUTE:-10}
      - DATABASE_URL=postgresql://${POSTGRES_USER:-vestigo}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-vestigo}
      - WEBHOOK_URL=${WEBHOOK_URL:-}
      - WEBHOOK_MIN_SEVERITY=${WEBHOOK_MIN_SEVERITY:-high}
    depends_on:
      parser-service:
        condition: service_healthy
      enricher-service:
        condition: service_healthy
      ai-service:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - vestigo-net
    restart: unless-stopped

  parser-service:
    build: ./parser-service
    expose:
      - "8001"
    environment:
      - INTERNAL_API_SECRET=${INTERNAL_API_SECRET}
    networks:
      - vestigo-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  enricher-service:
    build: ./enricher-service
    expose:
      - "8002"
    environment:
      - INTERNAL_API_SECRET=${INTERNAL_API_SECRET}
      - ABUSEIPDB_API_KEY=${ABUSEIPDB_API_KEY}
      - VIRUSTOTAL_API_KEY=${VIRUSTOTAL_API_KEY}
    networks:
      - vestigo-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8002/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  ai-service:
    build: ./ai-service
    expose:
      - "8003"
    environment:
      - INTERNAL_API_SECRET=${INTERNAL_API_SECRET}
      - GROQ_API_KEY=${GROQ_API_KEY}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - LLM_PROVIDER=${LLM_PROVIDER:-groq}
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL:-llama3.1:8b}
    networks:
      - vestigo-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8003/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  ollama:
    image: ollama/ollama:latest
    profiles: ["ollama"]
    expose:
      - "11434"
    volumes:
      - ollama_data:/root/.ollama
    environment:
      - OLLAMA_MODEL=${OLLAMA_MODEL:-llama3.1:8b}
    entrypoint: >
      /bin/sh -c "ollama serve &
      sleep 5 &&
      echo 'Baixando modelo ${OLLAMA_MODEL:-llama3.1:8b}...' &&
      ollama pull ${OLLAMA_MODEL:-llama3.1:8b} &&
      echo 'Modelo pronto.' &&
      wait"
    networks:
      - vestigo-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 20
      start_period: 300s

  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=${POSTGRES_DB:-vestigo}
      - POSTGRES_USER=${POSTGRES_USER:-vestigo}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - vestigo-net
    expose:
      - "5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-vestigo}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  vestigo-net:
    driver: bridge

volumes:
  postgres_data:
  ollama_data:
